namespace SubversionToGit
{
    /*
        using System;

        static class EnvironmentCommand
        {
            private const string AppName = "export";
            private const string format_iso = "yyyy-MM-ddTHH:mm:ss";

            public static ShellCommand SetAuthorDate(DateTime authorDate)
            {
                string args = $"GIT_AUTHOR_DATE=\"{authorDate.ToString(format_iso)}\"";
                return new ShellCommand(AppName, args);
            }

            public static ShellCommand SetCommitterDate(DateTime committerDate)
            {
                string args = $"GIT_COMMITTER_DATE=\"{committerDate.ToString(format_iso)}\"";
                return new ShellCommand(AppName, args);
            }
        }
    */
}
